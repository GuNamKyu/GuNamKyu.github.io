

while (조건){
    반복해서 실행할 코드
    }


<br /> = 줄바꿈

for(초기화; 반복조건; 반복이 될 때마다 실행되는 코드){
    반복해서 실행될 코드
   }

i = i +1; 는 i++; ++i 와 비슷하다. 대개 i++을 많이 사용한다.
하지만 i++ 는 기존의 값을 먼저 사용하고 ++i는 1을 더한 값을 먼저 사용한다는 점에서 다르다
ex) i = 0;             i=0;
    alert(i++);        alert(++i);
    => 0              => 1

break는 반복작업의 중단

반복문(조건문) or 조건문(반복문) 가능

문자와 문자사이의 숫자는 자동으로 문자열로 변환

객체는 값을 담는 그릇
배열은 순서를 가짐 0,1,2..., 그러나 객체 안에 담긴 값은 순서가 없음
값으로서의 함수는 객체안에 저장될 수 있음